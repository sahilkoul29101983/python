#!/usr/bin/env python3

# Get the user to input the value of Hostname object
hostname = input("What value should we set for the hostname?")
if hostname == "MTG":
    print("The hostname was found to be MTG")
